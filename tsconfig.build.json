{
  "//": "Used only by `pnpm build` (see typescript.tsconfigPath in next.config.ts). Identical to tsconfig.json except it ignores .next/dev, the route types `next dev` generates — those include the dev-only /admin routes and clash with the production route map.",
  "extends": "./tsconfig.json",
  "include": [
    "next-env.d.ts",
    "**/*.ts",
    "**/*.tsx",
    ".next/types/**/*.ts",
    "**/*.mts"
  ],
  "exclude": ["node_modules", ".next/dev"]
}
